server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # Hashed build assets — long cache
  location /assets/ {
    try_files $uri =404;
    add_header Cache-Control "public, max-age=31536000, immutable";
  }

  # Other static files in root (favicon, css for policy pages, etc.)
  location ~* \.(?:svg|ico|png|webp|woff2?|css)$ {
    try_files $uri =404;
    add_header Cache-Control "public, max-age=604800";
  }

  # Policy / marketing HTML pages (must not fall back to SPA shell)
  location ~* \.(?:html)$ {
    try_files $uri =404;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
  }

  # SPA shell — never cache (prevents white screen after deploy)
  location / {
    try_files $uri $uri/ /index.html;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    add_header Pragma "no-cache" always;
    add_header Expires "0" always;
  }
}
